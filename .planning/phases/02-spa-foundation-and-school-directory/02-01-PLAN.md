---
phase: 02-spa-foundation-and-school-directory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - Trunk.toml
  - index.html
  - build.rs
  - src/main.rs
  - src/app.rs
  - src/models.rs
  - src/state.rs
  - src/components/mod.rs
  - src/pages/mod.rs
  - src/pages/listing.rs
  - src/pages/detail.rs
  - src/pages/not_found.rs
  - public/styles.css
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03

must_haves:
  truths:
    - "Running `trunk build` produces a dist/ directory with index.html and .wasm file"
    - "build.rs reads all 106 data/schools/*.yaml files and produces a valid schools.json in OUT_DIR"
    - "The app shell renders in a browser with two routes: / and /school/:id"
    - "Pushing to main triggers a GitHub Actions workflow that builds and deploys to GitHub Pages"
  artifacts:
    - path: "Cargo.toml"
      provides: "Rust project configuration with leptos 0.8 CSR dependencies"
      contains: 'leptos = { version = "0.8", features = ["csr"] }'
    - path: "build.rs"
      provides: "YAML-to-JSON conversion at compile time"
      contains: "serde_saphyr::from_str"
    - path: "src/models.rs"
      provides: "School, RatingEntry, LanguageEntry, AdmissionRequirements, Coords structs"
      exports: ["School", "load_schools"]
    - path: "src/app.rs"
      provides: "App component with Router, Routes, and two Route entries"
      contains: "path!(\"/school/:id\")"
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD pipeline for GitHub Pages deployment"
      contains: "trunk build --release"
  key_links:
    - from: "build.rs"
      to: "data/schools/*.yaml"
      via: "fs::read_dir + serde_saphyr deserialization"
      pattern: "read_dir.*data/schools"
    - from: "src/models.rs"
      to: "build.rs output"
      via: "include_str!(concat!(env!(\"OUT_DIR\"), \"/schools.json\"))"
      pattern: "include_str!"
    - from: "src/app.rs"
      to: "src/pages/listing.rs"
      via: "Route path=path!(\"/\") view=ListingPage"
      pattern: "ListingPage"
---

<objective>
Bootstrap the entire Rust/Leptos WASM SPA project from scratch: project scaffold, build-time data embedding (106 school YAML files to JSON), app shell with routing, and GitHub Actions deployment workflow.

Purpose: Nothing else in Phase 2 can proceed without a compilable Rust project that loads school data and has routing. This plan establishes the foundation.
Output: A working `trunk serve` dev server showing a placeholder listing page and detail page, with all 106 schools loaded from embedded JSON. A GitHub Actions workflow ready to deploy on push to main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spa-foundation-and-school-directory/02-CONTEXT.md
@.planning/phases/02-spa-foundation-and-school-directory/02-RESEARCH.md

Data contract (source of truth for school fields):
@pipeline/models.py
@data/schools/01P03.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust project scaffold with Cargo.toml, Trunk config, index.html, and build.rs data embedding</name>
  <files>
    Cargo.toml,
    rust-toolchain.toml,
    Trunk.toml,
    index.html,
    build.rs,
    public/styles.css
  </files>
  <read_first>
    pipeline/models.py (canonical SchoolRecord schema — Rust structs must mirror these fields),
    data/schools/01P03.yaml (real data example — verify serde deserialization handles this shape),
    .planning/phases/02-spa-foundation-and-school-directory/02-RESEARCH.md (Cargo.toml template, index.html template, build.rs pattern, Trunk config)
  </read_first>
  <action>
    **Install Rust toolchain** (if not already installed):
    ```
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain nightly
    rustup target add wasm32-unknown-unknown --toolchain nightly
    cargo install --locked trunk
    ```

    **Create rust-toolchain.toml** per D-20 and Pitfall 6 (pin nightly):
    ```toml
    [toolchain]
    channel = "nightly"
    targets = ["wasm32-unknown-unknown"]
    ```

    **Create Cargo.toml** with exact dependencies from RESEARCH.md:
    ```toml
    [package]
    name = "berlin-gymnasien"
    version = "0.1.0"
    edition = "2024"

    [dependencies]
    leptos = { version = "0.8", features = ["csr"] }
    leptos_router = "0.8"
    leptos_meta = "0.8"
    serde = { version = "1", features = ["derive"] }
    serde_json = "1"
    wasm-bindgen = "0.2"
    web-sys = { version = "0.3", features = ["Window", "Document", "HtmlElement"] }
    console_log = "1"
    console_error_panic_hook = "0.1"
    log = "0.4"

    [build-dependencies]
    serde = { version = "1", features = ["derive"] }
    serde-saphyr = "0.0.10"
    serde_json = "1"

    [profile.release]
    opt-level = "z"
    lto = true
    codegen-units = 1
    panic = "abort"
    ```

    Note: Do NOT include `reactive_stores` or `leptos-use` yet — add them only when Plan 02 needs them. Keep initial compile lean per D-03/D-20.

    **Create Trunk.toml** per D-22:
    ```toml
    [build]
    dist = "dist"

    [serve]
    port = 8080
    ```

    **Create index.html** (Trunk entry point) per D-04 (German language):
    ```html
    <!DOCTYPE html>
    <html lang="de" dir="ltr">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Berliner Gymnasien Vergleich</title>
        <link data-trunk rel="css" href="public/styles.css" />
        <link data-trunk rel="rust" data-wasm-opt="z" />
    </head>
    <body></body>
    </html>
    ```

    **Create build.rs** that reads all `data/schools/*.yaml`, deserializes each with serde-saphyr, serializes all to a single JSON array, writes to `OUT_DIR/schools.json`. Must include `println!("cargo:rerun-if-changed=data/schools/")` per Pitfall 5. The build.rs School struct must include ALL fields from pipeline/models.py SchoolRecord with matching names and types (String for str, Option for Optional, Vec for list, HashMap for dict). Use `#[serde(default)]` on every optional/collection field so missing YAML fields don't cause parse errors. Sort schools alphabetically by name before serializing.

    **Create public/styles.css** — empty placeholder with a CSS reset (box-sizing, margin/padding zero on body) and a comment noting this will be populated in Plan 02.

    **Verify**: Run `cargo check` to confirm the build.rs compiles and produces schools.json. If Rust toolchain isn't installed, install it first.
  </action>
  <verify>
    <automated>cd /Users/andrey.rudenko/conductor/workspaces/berlin-gymnasium/amman && cargo check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `leptos = { version = "0.8", features = ["csr"] }`
    - Cargo.toml contains `serde-saphyr = "0.0.10"` under [build-dependencies]
    - Cargo.toml [profile.release] contains `opt-level = "z"` and `lto = true` and `codegen-units = 1`
    - rust-toolchain.toml contains `channel = "nightly"` and `targets = ["wasm32-unknown-unknown"]`
    - index.html contains `lang="de"` and `data-wasm-opt="z"`
    - build.rs contains `serde_saphyr::from_str` and `rerun-if-changed=data/schools`
    - build.rs School struct has fields: school_id, name, district, last_updated, address, coords, website, phone, email, traeger, student_count, teacher_count, accepts_after_4th_grade, profile, ganztag, languages, open_day, admission_requirements, abitur_average, ratings, completeness_score, data_sources
    - `cargo check` exits with code 0
  </acceptance_criteria>
  <done>Cargo.toml, rust-toolchain.toml, Trunk.toml, index.html, build.rs, and public/styles.css exist. `cargo check` succeeds, meaning build.rs reads and converts all 106 YAML files to JSON at compile time.</done>
</task>

<task type="auto">
  <name>Task 2: Rust data models, app shell with router, and placeholder pages</name>
  <files>
    src/main.rs,
    src/app.rs,
    src/models.rs,
    src/state.rs,
    src/components/mod.rs,
    src/pages/mod.rs,
    src/pages/listing.rs,
    src/pages/detail.rs,
    src/pages/not_found.rs
  </files>
  <read_first>
    pipeline/models.py (canonical field definitions — Rust models.rs must mirror),
    build.rs (just created — verify the JSON schema that models.rs must deserialize matches),
    .planning/phases/02-spa-foundation-and-school-directory/02-RESEARCH.md (Pattern 2: routing, Pattern 4: global state, main.rs template, app.rs template, models.rs template)
  </read_first>
  <action>
    **Create src/models.rs** with Rust structs mirroring pipeline/models.py SchoolRecord:
    - `School` struct with `#[derive(Clone, Debug, Serialize, Deserialize, PartialEq)]`
    - All fields from build.rs School struct but as the runtime model
    - Nested structs: `Coords { lat: f64, lng: f64 }`, `LanguageEntry { name: String, from_grade: Option<u32> }`, `AdmissionRequirements { notendurchschnitt: Option<f64>, oversubscribed: Option<bool>, selection_criteria: Option<String>, probeunterricht: Option<bool>, entrance_test: Option<bool>, notes: Option<String> }`, `RatingEntry { source: String, score: Option<f64>, scale_min: f64, scale_max: f64, review_count: Option<u32>, retrieved: String }` with `default_scale_min()` and `default_scale_max()` helper fns
    - `const SCHOOLS_JSON: &str = include_str!(concat!(env!("OUT_DIR"), "/schools.json"));`
    - `pub fn load_schools() -> Vec<School>` that deserializes SCHOOLS_JSON

    **Create src/state.rs** with `AppState`:
    - `pub struct AppState { pub schools: Vec<School> }`
    - `pub fn provide_app_state()` that calls `load_schools()` and provides it via `provide_context`

    **Create src/app.rs** with App component:
    - Calls `provide_app_state()`
    - `<Router>` wrapping `<Routes fallback=|| view! { <NotFound /> }>`
    - Route `path!("/")` -> `ListingPage`
    - Route `path!("/school/:id")` -> `DetailPage`

    **Create src/main.rs**:
    - `console_error_panic_hook::set_once();`
    - `console_log::init_with_level(log::Level::Debug).unwrap();`
    - `mount_to_body(app::App);`
    - Module declarations: `mod app; mod models; mod state; mod components; mod pages;`

    **Create src/pages/mod.rs** re-exporting: `pub mod listing; pub mod detail; pub mod not_found;`

    **Create src/pages/listing.rs** with `ListingPage` component:
    - Reads `AppState` from context via `use_context::<AppState>()`
    - Renders a heading "Berliner Gymnasien" and a `<p>` showing school count: "{N} Schulen gefunden"
    - Renders a simple `<ul>` list of school names as `<li>` items (placeholder for Plan 02's card grid)
    - Each `<li>` wraps an `<a href=format!("/school/{}", school.school_id)>` linking to detail page

    **Create src/pages/detail.rs** with `DetailPage` component:
    - Uses `use_params_map()` from leptos_router to get `:id` param
    - Reads `AppState` from context, finds school by school_id
    - Renders school name as `<h1>` and district as `<h2>` (placeholder for Plan 03's full detail)
    - Shows "Schule nicht gefunden" if school_id doesn't match
    - Has a back link `<a href="/">` with text "Zurueck zur Liste"

    **Create src/pages/not_found.rs** with `NotFound` component:
    - Renders "Seite nicht gefunden" heading

    **Create src/components/mod.rs** — empty, with comment noting Plan 02 will add components.

    **Verify**: Run `trunk build` (not `--release`) to confirm the full WASM app compiles and produces dist/index.html.
  </action>
  <verify>
    <automated>cd /Users/andrey.rudenko/conductor/workspaces/berlin-gymnasium/amman && trunk build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/models.rs contains `pub struct School` with field `pub school_id: String`
    - src/models.rs contains `pub fn load_schools() -> Vec<School>`
    - src/models.rs contains `include_str!(concat!(env!("OUT_DIR"), "/schools.json"))`
    - src/app.rs contains `path!("/school/:id")`
    - src/app.rs contains `ListingPage` and `DetailPage` references
    - src/main.rs contains `mount_to_body`
    - src/pages/listing.rs contains `Berliner Gymnasien` and `Schulen gefunden`
    - src/pages/detail.rs contains `use_params_map` and `Schule nicht gefunden`
    - `trunk build` exits with code 0 and dist/index.html exists
  </acceptance_criteria>
  <done>Full app shell compiles to WASM. `trunk build` succeeds. Two routes work: / shows school list count with linked names, /school/:id shows school name+district. All 106 schools loaded from embedded JSON.</done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions deployment workflow</name>
  <files>
    .github/workflows/deploy.yml
  </files>
  <read_first>
    .planning/phases/02-spa-foundation-and-school-directory/02-RESEARCH.md (GitHub Actions workflow template, D-21, D-22),
    Trunk.toml (verify dist directory configuration),
    index.html (verify Trunk entry point)
  </read_first>
  <action>
    **Create .github/workflows/deploy.yml** per D-21, D-22, and the RESEARCH.md template:

    ```yaml
    name: Deploy to GitHub Pages

    on:
      push:
        branches: ["main"]

    permissions:
      contents: read
      pages: write
      id-token: write

    concurrency:
      group: "pages"
      cancel-in-progress: false

    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4

          - name: Set up Rust nightly
            uses: dtolnay/rust-toolchain@nightly
            with:
              targets: wasm32-unknown-unknown

          - name: Cache cargo registry and build
            uses: actions/cache@v4
            with:
              path: |
                ~/.cargo/registry
                ~/.cargo/git
                target
              key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

          - name: Install Trunk
            run: cargo install --locked trunk

          - name: Build
            run: trunk build --release --public-url /amman/

          - name: Copy 404.html for SPA routing
            run: cp dist/index.html dist/404.html

          - name: Setup Pages
            uses: actions/configure-pages@v4

          - name: Upload artifact
            uses: actions/upload-pages-artifact@v4
            with:
              path: dist

      deploy:
        environment:
          name: github-pages
          url: ${{ steps.deployment.outputs.page_url }}
        runs-on: ubuntu-latest
        needs: build
        steps:
          - name: Deploy to GitHub Pages
            id: deployment
            uses: actions/deploy-pages@v4
    ```

    Key details:
    - Uses `dtolnay/rust-toolchain@nightly` (not rustup-init) per CI best practice
    - `--public-url /amman/` per D-22 so assets resolve on GitHub Pages subdirectory
    - `cp dist/index.html dist/404.html` per Pitfall 1 (path-based routing fallback — replaces D-17 hash routing which is not available in Leptos 0.8)
    - Cargo cache to speed up rebuilds
    - `actions/deploy-pages@v4` for atomic GitHub Pages deployment
  </action>
  <verify>
    <automated>cat /Users/andrey.rudenko/conductor/workspaces/berlin-gymnasium/amman/.github/workflows/deploy.yml | grep -c "trunk build --release --public-url /amman/"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml exists
    - deploy.yml contains `on: push: branches: ["main"]`
    - deploy.yml contains `trunk build --release --public-url /amman/`
    - deploy.yml contains `cp dist/index.html dist/404.html`
    - deploy.yml contains `actions/deploy-pages@v4`
    - deploy.yml contains `dtolnay/rust-toolchain@nightly` with `targets: wasm32-unknown-unknown`
    - deploy.yml contains `permissions: pages: write`
  </acceptance_criteria>
  <done>GitHub Actions workflow exists and is configured to build the Leptos SPA with Trunk and deploy to GitHub Pages on push to main. 404.html fallback handles SPA routing.</done>
</task>

</tasks>

<verification>
1. `cargo check` exits 0 — Rust project compiles, build.rs processes all 106 YAML files
2. `trunk build` exits 0 — WASM binary and dist/ directory produced
3. `ls dist/` shows index.html and at least one .wasm file
4. `.github/workflows/deploy.yml` exists with correct trunk build command
5. `grep -c "school_id" src/models.rs` returns at least 1 (School struct has school_id field)
6. `grep "include_str!" src/models.rs` shows the OUT_DIR/schools.json embedding
</verification>

<success_criteria>
- Rust/Leptos SPA project compiles and produces a working WASM binary (DEPL-01)
- All 106 school YAML files are converted to JSON and embedded at build time (DEPL-03)
- App shell has two working routes: / (listing placeholder) and /school/:id (detail placeholder)
- GitHub Actions workflow is ready to deploy to GitHub Pages on push to main (DEPL-02)
- `trunk serve` shows the app in a browser with school data loaded
</success_criteria>

<output>
After completion, create `.planning/phases/02-spa-foundation-and-school-directory/02-01-SUMMARY.md`
</output>
