---
phase: 01-data-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pipeline/seed.py
  - data/schools_index.yaml
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "Running `just seed` fetches all Berlin Gymnasien from the WFS and writes data/schools_index.yaml"
    - "schools_index.yaml contains all 95+ public Gymnasien with school_id, name, district, coords (lat/lng), address, phone, email, website, and traeger fields"
    - "Student and teacher counts from the Eckdaten XLSX are merged into each school entry by BSN"
    - "WFS coordinates are in WGS84 (lat values ~52.x, lng values ~13.x) — not EPSG:25833"
    - "School count in schools_index.yaml is between 95 and 110 (covers public + private if include_private_schools=true)"
  artifacts:
    - path: "pipeline/seed.py"
      provides: "WFS fetch + XLSX merge → schools_index.yaml"
      exports:
        - "async def run_seed(config: dict) -> list[dict]"
    - path: "data/schools_index.yaml"
      provides: "Canonical list of all Berlin Gymnasien (produced at runtime)"
      contains: "school_id"
  key_links:
    - from: "pipeline/seed.py"
      to: "https://gdi.berlin.de/services/wfs/schulen"
      via: "httpx.AsyncClient GET with srsName=EPSG:4326 and CQL_FILTER=schulart='Gymnasium'"
      pattern: "srsName.*EPSG:4326"
    - from: "pipeline/seed.py"
      to: "https://www.berlin.de/sen/bildung/service/daten/od-eckdaten-allg-2024.xlsx"
      via: "httpx.AsyncClient GET + openpyxl.load_workbook"
      pattern: "openpyxl\\.load_workbook"
---

<objective>
Implement the seed step: fetch all Berlin Gymnasien from the GDI Berlin WFS endpoint, merge student/teacher counts from the Eckdaten XLSX, and write `data/schools_index.yaml` as the canonical school list.

Purpose: schools_index.yaml is the input to every downstream step. It provides school_id (BSN), coordinates, address, contact info, and structured counts for all ~100+ Gymnasien without any AI involvement.

Output: `pipeline/seed.py` (runnable module), `data/schools_index.yaml` (produced at runtime by `just seed`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-pipeline/01-CONTEXT.md
@.planning/phases/01-data-pipeline/01-RESEARCH.md
@.planning/phases/01-data-pipeline/01-01-SUMMARY.md
@pipeline/models.py
@pipeline/pipeline.yaml

<interfaces>
<!-- WFS fetch pattern from RESEARCH.md Pattern 1 — implement exactly this -->

WFS endpoint: https://gdi.berlin.de/services/wfs/schulen
Required params:
  service: "WFS"
  version: "2.0.0"
  request: "GetFeature"
  typeNames: "schulen"
  outputFormat: "application/json"
  srsName: "EPSG:4326"           # CRITICAL: omitting this returns EPSG:25833 (UTM)
  CQL_FILTER: "schulart='Gymnasium'"

WFS feature.properties fields available:
  bsn          → school_id
  schulname    → name (strip whitespace)
  bezirk       → district
  strasse      → street
  hausnr       → house number (strip)
  plz          → postal code → build address: "{strasse} {hausnr}, {plz} Berlin"
  telefon      → phone
  email        → email
  internet     → website
  traeger      → "öffentlich" or "privat"

WFS feature.geometry.coordinates: [lng, lat]  (GeoJSON order)
→ coords = {"lat": coords[1], "lng": coords[0]}

<!-- Eckdaten XLSX fetch pattern from RESEARCH.md Code Examples -->

Eckdaten URL: https://www.berlin.de/sen/bildung/service/daten/od-eckdaten-allg-2024.xlsx
Parse with openpyxl.load_workbook(io.BytesIO(content))
BSN column header: "BSN" or "Schulnummer" (check both)
Merge by matching BSN → school_id
Fields to extract: student_count (Schüler), teacher_count (Lehrkräfte)
  Check header names in the actual sheet; may vary.

<!-- include_private_schools from pipeline.yaml -->
If include_private_schools=true (default): include all traeger values.
If false: filter to traeger='öffentlich' only.

<!-- schools_index.yaml structure -->
Each entry is a dict with fields:
  school_id, name, district, address, coords, website, phone, email,
  traeger, student_count, teacher_count
Missing fields: set to null (not omitted) in schools_index.yaml
  (schools_index is an intermediate file, not a SchoolRecord — null OK here)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement pipeline/seed.py — WFS fetch + Eckdaten XLSX merge</name>
  <read_first>
    - /Users/andrey.rudenko/conductor/workspaces/berlin-gymnasium/amman/.planning/phases/01-data-pipeline/01-RESEARCH.md (Pattern 1: WFS Seed with EPSG:4326, lines 164–204; Code Examples: Reading Eckdaten XLSX, lines 481–503)
    - /Users/andrey.rudenko/conductor/workspaces/berlin-gymnasium/amman/pipeline/models.py
    - /Users/andrey.rudenko/conductor/workspaces/berlin-gymnasium/amman/pipeline/pipeline.yaml
  </read_first>
  <files>pipeline/seed.py</files>
  <action>
Create `pipeline/seed.py` implementing the seed step. The module must be runnable as a standalone step (`python pipeline/seed.py`) and importable by run.py.

Key requirements:
1. Use `httpx.AsyncClient` for all HTTP (non-blocking, consistent with pipeline architecture).
2. WFS request MUST include `srsName=EPSG:4326` (Pitfall 1 — WFS default is EPSG:25833).
3. XLSX fetch: download with httpx, parse with `openpyxl.load_workbook(io.BytesIO(content))`.
4. Merge by BSN: XLSX BSN may be integer; WFS BSN is string. Normalize both to string for matching.
5. Respect `include_private_schools` config key.
6. Write `data/schools_index.yaml` using ruamel.yaml.
7. Print summary to stdout: total schools fetched, private schools count, XLSX merge hits.

```python
"""
pipeline/seed.py — Seed step: WFS fetch + Eckdaten XLSX merge.

Produces: data/schools_index.yaml
Run via: just seed  (or: uv run python pipeline/seed.py)
"""
from __future__ import annotations

import asyncio
import io
import logging
from pathlib import Path
from typing import Any

import httpx
import openpyxl
from ruamel.yaml import YAML

logger = logging.getLogger(__name__)

WFS_URL = "https://gdi.berlin.de/services/wfs/schulen"
ECKDATEN_URL = "https://www.berlin.de/sen/bildung/service/daten/od-eckdaten-allg-2024.xlsx"


async def fetch_gymnasien(include_private: bool = True) -> list[dict]:
    """Fetch all Berlin Gymnasien from GDI Berlin WFS. Returns list of school dicts."""
    params = {
        "service": "WFS",
        "version": "2.0.0",
        "request": "GetFeature",
        "typeNames": "schulen",
        "outputFormat": "application/json",
        "srsName": "EPSG:4326",           # CRITICAL: always include — prevents EPSG:25833 default
        "CQL_FILTER": "schulart='Gymnasium'",
    }
    async with httpx.AsyncClient(timeout=30) as client:
        resp = await client.get(WFS_URL, params=params)
        resp.raise_for_status()
        data = resp.json()

    schools = []
    for feature in data["features"]:
        props = feature["properties"]
        traeger = props.get("traeger", "")
        if not include_private and traeger != "öffentlich":
            continue
        # GeoJSON coordinates are [lng, lat]
        lng, lat = feature["geometry"]["coordinates"]
        hausnr = (props.get("hausnr") or "").strip()
        address = f"{props.get('strasse', '')} {hausnr}, {props.get('plz', '')} Berlin".strip()
        schools.append({
            "school_id": str(props["bsn"]),
            "name": (props.get("schulname") or "").strip(),
            "district": props.get("bezirk"),
            "address": address or None,
            "coords": {"lat": lat, "lng": lng},
            "website": props.get("internet") or None,
            "phone": props.get("telefon") or None,
            "email": props.get("email") or None,
            "traeger": traeger or None,
            "student_count": None,   # filled by XLSX merge below
            "teacher_count": None,
        })
    return schools


async def fetch_eckdaten() -> dict[str, dict]:
    """
    Fetch Eckdaten XLSX and return dict keyed by BSN (str) with student/teacher counts.
    Returns empty dict if fetch fails (non-blocking — XLSX is optional data).
    """
    async with httpx.AsyncClient(timeout=30) as client:
        try:
            resp = await client.get(ECKDATEN_URL)
            resp.raise_for_status()
        except httpx.HTTPError as e:
            logger.warning("Eckdaten XLSX fetch failed: %s — student/teacher counts will be null", e)
            return {}

    wb = openpyxl.load_workbook(io.BytesIO(resp.content))
    ws = wb.active
    headers = [str(cell.value).strip() if cell.value else "" for cell in next(ws.iter_rows(min_row=1, max_row=1))]

    # Find BSN, student count, and teacher count column indices
    # Column names vary — check multiple possibilities
    bsn_col = next((i for i, h in enumerate(headers) if h in ("BSN", "Schulnummer", "bsn")), None)
    student_col = next((i for i, h in enumerate(headers) if "Schüler" in h or "schueler" in h.lower() or "SuS" in h), None)
    teacher_col = next((i for i, h in enumerate(headers) if "Lehrkräfte" in h or "Lehrer" in h or "lehrkraefte" in h.lower()), None)

    if bsn_col is None:
        logger.warning("BSN column not found in Eckdaten XLSX. Headers: %s", headers[:10])
        return {}

    result = {}
    for row in ws.iter_rows(min_row=2, values_only=True):
        bsn = row[bsn_col]
        if bsn is None:
            continue
        entry: dict[str, Any] = {}
        if student_col is not None and row[student_col] is not None:
            try:
                entry["student_count"] = int(row[student_col])
            except (ValueError, TypeError):
                pass
        if teacher_col is not None and row[teacher_col] is not None:
            try:
                entry["teacher_count"] = int(row[teacher_col])
            except (ValueError, TypeError):
                pass
        result[str(bsn).strip()] = entry
    return result


def merge_eckdaten(schools: list[dict], eckdaten: dict[str, dict]) -> list[dict]:
    """Merge student/teacher counts into school list by BSN (school_id)."""
    merged = 0
    for school in schools:
        sid = school["school_id"]
        if sid in eckdaten:
            school["student_count"] = eckdaten[sid].get("student_count")
            school["teacher_count"] = eckdaten[sid].get("teacher_count")
            merged += 1
    logger.info("Eckdaten merge: %d/%d schools matched by BSN", merged, len(schools))
    return schools


def write_index(schools: list[dict], index_path: Path) -> None:
    """Write schools list to YAML index file."""
    index_path.parent.mkdir(parents=True, exist_ok=True)
    yaml = YAML()
    yaml.default_flow_style = False
    yaml.width = 200
    with index_path.open("w", encoding="utf-8") as f:
        yaml.dump(schools, f)


async def run_seed(config: dict) -> list[dict]:
    """
    Run the seed step. Returns list of school dicts.
    Called by pipeline/run.py orchestrator.
    """
    include_private = config.get("include_private_schools", True)
    index_path = Path(config.get("index_file", "data/schools_index.yaml"))

    logger.info("Fetching Gymnasien from WFS (include_private=%s)...", include_private)
    schools = await fetch_gymnasien(include_private=include_private)
    logger.info("WFS returned %d schools", len(schools))

    # Validate coordinates are WGS84 (Pitfall 1 detection)
    bad_coords = [s for s in schools if s["coords"]["lat"] > 1000 or s["coords"]["lng"] > 1000]
    if bad_coords:
        raise ValueError(
            f"WFS returned non-WGS84 coordinates for {len(bad_coords)} schools. "
            "Check srsName=EPSG:4326 parameter."
        )

    logger.info("Fetching Eckdaten XLSX for student/teacher counts...")
    eckdaten = await fetch_eckdaten()
    schools = merge_eckdaten(schools, eckdaten)

    write_index(schools, index_path)
    logger.info("Written schools_index.yaml with %d schools → %s", len(schools), index_path)

    private_count = sum(1 for s in schools if s.get("traeger") != "öffentlich")
    public_count = len(schools) - private_count
    print(f"Seed complete: {len(schools)} schools ({public_count} public, {private_count} private)")
    print(f"Eckdaten merge: {sum(1 for s in schools if s['student_count'])} schools with counts")
    print(f"Output: {index_path}")

    return schools


if __name__ == "__main__":
    import sys
    logging.basicConfig(level=logging.INFO, format="%(levelname)s %(message)s")
    # Load config from pipeline.yaml
    yaml = YAML()
    config_path = Path("pipeline/pipeline.yaml")
    if not config_path.exists():
        config_path = Path("pipeline.yaml")
    config = yaml.load(config_path) if config_path.exists() else {}
    asyncio.run(run_seed(config))
    sys.exit(0)
```
  </action>
  <verify>
    <automated>cd /Users/andrey.rudenko/conductor/workspaces/berlin-gymnasium/amman && python -c "import ast, sys; ast.parse(open('pipeline/seed.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pipeline/seed.py` contains `srsName.*EPSG:4326` (grep: `grep "EPSG:4326" pipeline/seed.py`)
    - `pipeline/seed.py` contains `openpyxl.load_workbook` (grep: `grep "openpyxl" pipeline/seed.py`)
    - `pipeline/seed.py` defines `async def run_seed(config: dict)` (grep: `grep "async def run_seed" pipeline/seed.py`)
    - `pipeline/seed.py` contains coordinate validation raising ValueError for non-WGS84 coords (grep: `grep "non-WGS84" pipeline/seed.py`)
    - `pipeline/seed.py` contains `async def fetch_gymnasien` (grep: `grep "async def fetch_gymnasien" pipeline/seed.py`)
    - `pipeline/seed.py` contains `async def fetch_eckdaten` (grep: `grep "async def fetch_eckdaten" pipeline/seed.py`)
    - Python syntax check passes (automated verify above exits 0)
  </acceptance_criteria>
  <done>pipeline/seed.py is implemented and syntactically valid. `just seed` will call run_seed() and write data/schools_index.yaml with all Gymnasien from the WFS including XLSX-merged counts.</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('pipeline/seed.py').read()); print('OK')"` exits 0
2. `grep "EPSG:4326" pipeline/seed.py` finds the srsName parameter
3. `grep "async def run_seed" pipeline/seed.py` finds the function
4. (Runtime, requires ANTHROPIC_API_KEY and network): `just seed` produces `data/schools_index.yaml` with 95–110 school entries
</verification>

<success_criteria>
- `pipeline/seed.py` is syntactically valid Python
- WFS fetch uses `srsName=EPSG:4326` parameter
- Eckdaten XLSX fetch uses `openpyxl.load_workbook(io.BytesIO(...))`
- Merge logic matches BSN strings (normalizes int BSN from XLSX to str)
- Coordinate sanity check raises ValueError if coords > 1000 (EPSG:25833 detection)
- `run_seed(config)` is the public interface for the orchestrator
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-02-SUMMARY.md` following the summary template.
</output>
