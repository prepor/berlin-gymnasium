---
phase: 01-data-pipeline
plan: 01
subsystem: pipeline
tags: [python, pydantic, uv, justfile, yaml, schema]

# Dependency graph
requires: []
provides:
  - "pipeline/models.py: SchoolRecord, RatingEntry, LanguageEntry, AdmissionRequirements Pydantic models"
  - "pipeline/pyproject.toml: uv-managed Python env with 8 pinned dependencies"
  - "pipeline/pipeline.yaml: pipeline config with WFS URL, semaphore_limit, batch_size, cache_dir"
  - "justfile: 7 pipeline task targets (seed, scrape, enrich, validate, write, all, force)"
  - ".gitignore: Python/cache exclusions"
affects: [02-seed, 03-enrich, 04-validate, 05-write, all downstream pipeline modules]

# Tech tracking
tech-stack:
  added:
    - "anthropic==0.86.0"
    - "httpx==0.28.1"
    - "beautifulsoup4==4.14.3"
    - "pydantic==2.12.5"
    - "ruamel.yaml==0.19.1"
    - "tenacity==9.1.4"
    - "deepdiff==8.6.2"
    - "openpyxl==3.1.5"
    - "uv (package manager)"
    - "just (task runner)"
  patterns:
    - "All pipeline modules import from pipeline.models — single source of truth for schema"
    - "Optional fields with empty defaults (no null in YAML) for clean diffs"
    - "_pinned_fields alias for manual override support"
    - "uv run --project pipeline from workspace root for correct venv resolution"

key-files:
  created:
    - "pipeline/models.py"
    - "pipeline/pyproject.toml"
    - "pipeline/pipeline.yaml"
    - "pipeline/__init__.py"
    - "pipeline/uv.lock"
    - "justfile"
    - ".gitignore"
    - "pipeline/cache/.gitkeep"
    - "data/schools/.gitkeep"
  modified: []

key-decisions:
  - "Use hatchling packages=['.'] so the pipeline/ dir is the package root when synced from pipeline/"
  - "Add --project pipeline to all justfile uv run commands so they resolve the venv from workspace root"
  - "_pinned_fields uses Pydantic alias so field serializes as _pinned_fields in YAML"

patterns-established:
  - "Pattern 1: uv run --project pipeline python pipeline/run.py for all pipeline invocations"
  - "Pattern 2: from pipeline.models import SchoolRecord — import path for all downstream modules"
  - "Pattern 3: Field(default_factory=list/dict) for mutable default fields in Pydantic models"

requirements-completed: [DATA-04, DATA-05, DATA-07, DATA-08, DATA-09, DATA-10, DATA-11, DATA-12]

# Metrics
duration: 4min
completed: 2026-03-26
---

# Phase 1 Plan 1: Bootstrap Data Pipeline Summary

**uv-managed Python pipeline scaffold with Pydantic SchoolRecord schema (8 fields for DATA-04/09/10/11/12), justfile task runner, and pipeline.yaml config — schema contract for all downstream pipeline modules**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-26T14:40:44Z
- **Completed:** 2026-03-26T14:44:44Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Python pipeline package (uv-managed) with all 8 pinned dependencies installed and lock file generated
- SchoolRecord Pydantic model with all required fields: school_id, name, district, last_updated, accepts_after_4th_grade, ratings (DATA-09), admission_requirements (DATA-11), open_day (DATA-12), provenance tracking (data_sources, unverified_fields, field_confidence)
- justfile with 7 targets: seed, scrape, enrich, validate, write, all, force — runnable from workspace root
- pipeline.yaml config with all 9 keys: WFS URL, Eckdaten URL, semaphore_limit, batch_size, max_web_searches, cache_dir, data_dir, index_file, log_level

## Task Commits

Each task was committed atomically:

1. **Task 1: Python project scaffold** - `8a697f7` (chore)
2. **Task 2: Pydantic schema models** - `99ed223` (feat)

## Files Created/Modified
- `pipeline/models.py` - SchoolRecord, RatingEntry, LanguageEntry, AdmissionRequirements Pydantic models
- `pipeline/pyproject.toml` - uv project config with 8 pinned dependencies + hatchling build config
- `pipeline/pipeline.yaml` - pipeline configuration defaults
- `pipeline/__init__.py` - empty package init
- `pipeline/uv.lock` - dependency lock file generated by uv sync
- `justfile` - 7 pipeline task runner targets using uv run
- `.gitignore` - Python/cache exclusions (.venv/, *.pyc, .env, cache/*.json)
- `pipeline/cache/.gitkeep` - tracked empty cache directory
- `data/schools/.gitkeep` - tracked empty output directory

## Decisions Made
- Used hatchling `packages = ["."]` config since the pyproject.toml lives inside the `pipeline/` directory (the package root is the project root)
- Added `--project pipeline` to all justfile `uv run` commands so venv resolves correctly when running from workspace root
- `_pinned_fields` uses Pydantic `alias="_pinned_fields"` so the field name in YAML matches the intended `_pinned_fields` convention from the plan spec

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added hatchling packages config to pyproject.toml**
- **Found during:** Task 2 (uv sync)
- **Issue:** `uv sync` failed with "Unable to determine which files to ship" — hatchling couldn't find the package because the project name `berlin-gymnasien-pipeline` doesn't match directory name `pipeline`
- **Fix:** Added `[tool.hatch.build.targets.wheel] packages = ["."]` to pyproject.toml
- **Files modified:** pipeline/pyproject.toml
- **Verification:** `uv sync --quiet` completes without error; `from pipeline.models import SchoolRecord` imports successfully
- **Committed in:** 99ed223 (Task 2 commit)

**2. [Rule 1 - Bug] Added --project pipeline to justfile uv run commands**
- **Found during:** Task 2 (verifying uv run from workspace root)
- **Issue:** `uv run python pipeline/run.py` from workspace root without `--project pipeline` uses system Python without pipeline venv — pydantic and other deps would not be found
- **Fix:** Updated all 7 justfile targets to use `uv run --project pipeline python pipeline/run.py`
- **Files modified:** justfile
- **Verification:** `uv run --project pipeline python -c "from pipeline.models import SchoolRecord; print('OK')"` succeeds from workspace root
- **Committed in:** 99ed223 (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes required for the pipeline to work correctly. No scope creep. Acceptance criteria pattern `grep "step seed" justfile` still passes.

## Issues Encountered
None beyond the auto-fixed deviations above.

## User Setup Required
None - no external service configuration required for this scaffold plan. ANTHROPIC_API_KEY will be needed for the `enrich` step in a later plan.

## Next Phase Readiness
- Schema contract established: all pipeline modules can now `from pipeline.models import SchoolRecord`
- `uv sync` in `pipeline/` creates the venv with all dependencies
- `just seed` / `just all` ready to invoke once `pipeline/run.py` is created in plan 02
- No blockers for next plan

---
*Phase: 01-data-pipeline*
*Completed: 2026-03-26*
